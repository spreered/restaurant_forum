
<div class="container">

  <ul class="nav nav-tabs">
    <li role="presentation" class="active" ><a href="#">首頁</a></li>
    <li role="presentation" ><%=link_to "最新動態", feeds_restaurants_path %>
    </li>
  </ul>
  <div class="row">
    <div class="category-nav">
      <ul class="nav nav-pills nav-pills-center">
        <li class="active" role="presentation"><%= link_to '全部餐廳', restaurants_path%></li>
        <% @categories.each do |category| %>
        <li role="presentation"><%=link_to category.name, category_path(category) %></li>
        <%end%>
      </ul> 
    </div>
  </div>
  <%= render partial: "shared/restaurant_list" %>
  <div class="clear-fix"></div>
    <div class="text-center">
      <%= paginate @restaurants %>      
    </div>


</div>